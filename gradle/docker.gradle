task buildDocker(dependsOn: 'build') {
    doLast {

        exec {
            commandLine 'docker', 'build', '-f', 'gradle/Dockerfile', '-t', 'somacode', 'build/libs/'
        }

        def springProfile = 'SPRING_PROFILES_ACTIVE=' + String.valueOf(profile)
        def pathStorage = '/root/somacode/storage/:/static/storage'

        exec {
            commandLine 'docker', 'run', '-d',
                    '--name', 'somacode',
                    '--network', 'somacode',
                    '-p', '8080:8080',
                    '-v', pathStorage,
                    '--restart', 'always',
                    '-e', springProfile, 'somacode'
        }

    }
}